name: Refresh headless observability manifest once

on:
  push:
    branches:
      - fix/todo-global-headless-observability-20260916

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/todo-global-headless-observability-20260916
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Validate focused tests
        run: |
          python -m pip install --disable-pip-version-check pytest
          python -m pytest tests/test_todo_gateway_windows_s4u.py -q -p no:cacheprovider
      - name: Remove one-shot workflow from index and refresh manifest
        run: |
          git rm --cached .github/workflows/refresh-manifest-headless-observability-once.yml
          rm -f .github/workflows/refresh-manifest-headless-observability-once.yml
          python scripts/generate_manifest.py
          python scripts/validate_rules.py --self-test
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --quiet || git commit -m 'chore: refresh manifest for headless observability'
          git push origin HEAD:fix/todo-global-headless-observability-20260916
